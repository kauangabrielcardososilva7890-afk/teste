# RECLAMAÇÕES DO DONO → TESTE QUE TRAVA (a lista viva)

**O que é este arquivo:** a ideia **A** ("transformar cada reclamação dele em teste"). A linha 19 é o caso inverso — uma **dúvida** dele ("não está aparecendo nenhum dado, é normal?") que virou trava para o comportamento certo **não mudar** sem ninguém perceber. Toda vez que o
dono reclamou de alguma coisa e a gente consertou, aquele conserto ganhou uma **trava automática**: se o
defeito voltar, a suíte fica vermelha antes de qualquer um usar o sistema.

**Quem cobra esta lista:** `test_reclamacoes_do_dono.js`. Ele lê a tabela abaixo e **reprova** se:

1. algum teste citado não existir mais no repositório;
2. algum teste citado não estiver registrado no `test_runner.js` (teste que existe mas ninguém roda não
   trava nada);
3. algum arquivo citado na coluna do conserto não existir; ou
4. uma das travas que moram aqui dentro (as linhas marcadas **aqui**) parar de valer.

**Como acrescentar uma reclamação nova:** conserto feito → escreva uma linha com as **palavras dele** (ou
o efeito, se ele só descreveu o sintoma), o que foi corrigido, onde vive a correção e o teste que trava.
Se ainda não existir teste, escreva a trava aqui dentro do `test_reclamacoes_do_dono.js` e marque
**aqui**.

| # | O que ele reclamou | Quando | O que foi corrigido | Onde vive a correção | Teste que trava |
|---|---|---|---|---|---|
| 1 | "o duplo clique nas telas não fazia nada" (5 telas copiaram a linha da tabela de produtos, e a variável da linha não existia) | 23/09 (r5) | cada tabela abre o seu próprio modal | `index.html` | `test_linhas_tabela_clique.js` |
| 2 | "MUDE A VERSÃO PARA V7.0.0 e CONFIRA TODOS OS ARQUIVOS para não dar problema" — e todo link do sistema levava para **código velho** (branch antiga) | 23/09 (r4) | versão igual em todos os arquivos + branch da sessão nos links | `package.json`, `index.html`, `mobile/www/index.html`, `importar.html`, `GUIA_DE_TESTE_NF.html`, `PASSO_A_PASSO_NUVEM_E_SITE.html`, `RELATORIO_DE_TESTE_NF.html` | `test_versao_visual.js`, `test_build_sync.js` + **aqui** |
| 3 | "NADA APARECEU NOS CONTRATOS NOVAMENTE, AS IMPRESSORAS, NADA" | 23/09 (r6/r9) | contrato/impressora não somem; recuperação varre **todos** os excluídos, em levas | `contratos_leituras_definitivo_patch.js`, `cloudflare_data_sync_patch.js` | `test_contrato_impressora_nao_some.js`, `test_recuperar_excluidos.js`, `test_recuperacao_completa.js`, `test_recuperacao_nao_ressuscita.js` |
| 4 | "não sabe o que é instantâneo já aparecer os dados? EU QUERO QUE MOSTRA INSTANTÂNEO SEM NENHUM ERRO" | 23/09 (r8/r9) | canal aberto com a nuvem (`/v1/changes/watch`) avisa o PC no mesmo instante; recuo sozinho para o ritmo de 3 s | `cloudflare_data_sync_patch.js` | `test_nuvem_rapida.js`, `test_cloudflare_data_sync.js` |
| 5 | "a tela ia secando / ficava na frente" | 23/09 (r10) | o defeito era no PC (foco/repintura), não na nuvem | `ajustes_v6108_lembrar_tela_patch.js` | `test_tela_nao_seca.js` |
| 6 | "pq fica voltando, tem como resolver? ... sobe rapidinho e o que foi feito depois demora ... vai subindo aos poucos" | 23/09 (r11) | fila elástica, gravação agrupada do estado, passe rápido na abertura | `cloudflare_data_sync_patch.js` | `test_persist.js`, `test_nuvem_rapida.js` |
| 7 | "os menus não estão ficando lá em cima no NF-e/NFC-e, lá que tem que ficar, e muda esse nome pra ser oficialmente o menu fiscal" | 19/09 (v6.1.0) | faixa viva re-injetada, nome oficial **Menu Fiscal**, os 6 itens sempre em cima | `menu_fiscal_oficial_patch.js`, `submenu_fiscal_oficial_patch.js`, `navegacao_fiscal_barra_escuro_patch.js` | **aqui** |
| 8 | "confira tudo… não está mudando nada, somente o rodapé da versão; o modo escuro é todo bugado" | 19/09 (v6.1.2) | acha o módulo fiscal pelo `onclick` (sobrevive a qualquer repintura) + CSS escuro próprio | `navegacao_fiscal_barra_escuro_patch.js` | **aqui** |
| 9 | "em usuários tem uma caixa que é 'o que são as 3 permissões?', retira isso" | 23/09 (v7.0.1) | o botão não é mais injetado (a explicação continua pronta, sem aparecer) | `permissoes_estorno_venda_patch.js` | **aqui** |
| 10 | "o `erro.txt` do rodapé **não pode voltar**" | 23/09 | o rodapé não tem mais esse botão (dentro do `.exe` o arquivo de erro continua existindo, mas não na cara do dono) | `index.html`, `mobile/www/index.html` | **aqui** |
| 11 | "senha: deixa a mesma, pois eu nunca nem compartilhei esse site direito" + "senha invisível" e a tela que listava usuários não mostrar nada | 23/09 (r7/r9) | senha não é mostrada nem trocada sozinha; a lista de exemplo não revela nada | `login_otimizacao_patch.js`, `ajustes_v5262_login_nuvem_primeiro_patch.js` | `test_login_sem_backdoor.js`, `test_senha_do_dono_manda.js` |
| 12 | "nada salvo no PC/navegador — só nuvem" (e a cópia local só quando a nuvem tem tudo) | 22/09 (v6.1.5, regra 44) | SÓ NUVEM: `saveDB` não grava no PC; cópia local só com `nuvemTemTudo()` confirmando | `cloudflare_data_sync_patch.js` | `test_exe_so_nuvem.js` + **aqui** |
| 13 | "dado que some/volta" (a dor nº 1 dele) | 24/09 (r23/r24) | a gravação entra na fila **no fim do clique**, a fila é gravada na hora, fechar não perde (varredura forçada + `keepalive`) e a fila ficou visível | `cloudflare_data_sync_patch.js` | `test_nuvem_nao_perde.js`, `bench_clique_nuvem.js` |
| 14 | "cada funçãozinha que tinha o sistema é útil, eu vou querer" (paridade) | 24/09 (r17/r18) | mapa de todas as camadas + teste que impede trocar função protegida sem levar a proteção | `mapa_camadas.js`, `MAPA_CAMADAS.md` | `test_camadas_protegidas.js` |
| 15 | senha do certificado A1 não pode ser pedida por `prompt` (regra 16: no `.exe` o `prompt` nativo **lança erro**) | 23/09 | modal do sistema em vez de `alert`/`confirm`/`prompt` | `nf_transmissao_patch.js`, `popup_sistema_patch.js` | `test_confirm_compat.js`, `test_ajustes_v6002.js` + **aqui** |
| 15b | (achada pela trava desta rodada, não por ele) o caminho fiscal ainda caía no `window.confirm` **nativo** quando o modal do sistema não estava disponível — em 2 pontos (nota duplicada e cancelamento em produção) | 24/09 (r25) | trocado por `confirmSistema` (modal do sistema) e, sem ele, **aviso na tela sem diálogo nativo**; no cancelamento em produção o padrão é **não cancelar** | `nf_transmissao_patch.js` | **aqui** |
| 16 | "a numeração estava ancorada em código morto" (a venda/notinha usava função de arquivo antigo) | 24/09 (r19) | a venda/OS/notinha passaram a usar o sistema vivo | `vendas_os_patch.js`, `notinha_patch.js` | **aqui** |
| 17 | venda faturada/estorno: só com permissão, e não pode sumir | 24/09 (r20) | portão de permissões em 11 funções + estorno que não perde a venda | `permissoes_estorno_venda_patch.js`, `ajustes_v5240_relatorio_grande_patch.js` | `test_camadas_protegidas.js`, `test_vos.js` |
| 18 | PIX da venda (valor/QR) tem que sair certo | 24/09 (r20) | PIX EMV com CRC16 (vetor do Banco Central) | `pix_patch.js`, `ajustes_v52219_pix_link_publico_patch.js` | `test_pix.js` |
| 22 | **"eu n to usando o teto gratis... e sim o pago"** (o dono, 24/09) — e ele estava certo: o **freio preventivo da nuvem** continuava com o número do plano GRÁTIS (95.000 linhas/dia) enquanto a aba Nuvem já mostrava o teto do PAGO. Numa conta paga a nuvem parava de aceitar gravação no meio do dia, o app dormia até as 21h e o que era digitado num PC não aparecia no outro | 24/09 (r28) | **corrigido na FONTE (motor da nuvem, v5.26.9):** o plano virou **uma fonte só** (`PLANO_PAGO`/`PLANO_GRATIS` → `PLANO`), o freio passou a valer 1.000.000/dia + 45.000.000/mês (teto real do pago) e o app reconhece o recado do mês (`monthly row write limit`) | `cloudflare-worker/src/index.js`, `cloudflare_data_sync_patch.js` | `test_worker_publico.js` (4 ✓ — inclui a CONTRA-PROVA de que 95.000 não barra mais), `test_sync_quota_guard.js` (3 ✓), `test_ajustes_v52280.js` (2 ✓) + **aqui** |
| 23 | (o que faltava para eu achar isso sem depender dele) a manutenção não tinha como **conferir de fora** se a nuvem estava recusando gravação | 24/09 (r28) | o `/health` (público, sem token e **sem expor dado nenhum de negócio**) passa a responder `freio` — plano, teto do dia, se disparou hoje, quando e o motivo; o **check-up** do dono mostra a mesma linha e ela entra no "Copiar resumo" | `cloudflare-worker/src/index.js`, `cloudflare_data_sync_patch.js`, `ajustes_v5227_nuvem_acompanhamento_patch.js` | `test_worker_publico.js` (2 ✓), `test_nuvem_explica.js` (3 ✓) + **aqui** |
| 24 | **"virou o PC?" / dado que some:** a **cópia local era liberada por CONTAGEM** (`nuvemTemTudo`: registros na nuvem >= registros daqui). Com registros SEGURADOS ("não enviar o que já existe aqui", que vivem só naquele PC) e a nuvem cheia, a conta fechava e a cópia local era apagada **com eles dentro** | 24/09 (r29) | **provado e corrigido na fonte:** liberação só com prova **registro por registro** (`known` + hash igual); qualquer registro sem prova, ou segurado, ou fila pendente → **não libera** | `cloudflare_data_sync_patch.js` | `test_nuvem_nao_perde.js` (2 casos novos; prova negativa: sem o conserto o teste fica VERMELHO com "cópia local APAGADA") + `test_ajustes_v6106.js` + **aqui** |
| 25 | **a nuvem recusou gravação e o app insistiu:** o **429 do freio de cota** era tratado como "nuvem ocupada" (`ehSobrecarga`), então o app repetia as tentativas e **queimava cota** em vez de dormir até a virada | 24/09 (r29) | marca `quota`/recado de limite sai do caminho de sobrecarga: cai no caminho do limite (dorme) e **relata** | `cloudflare_data_sync_patch.js` | `test_nuvem_explica.js` (caso "a nuvem recusou a gravação" → relato `freio`) + **aqui** |
| 26 | **"vc tem que falar o que falta"** — a manutenção não tinha como saber o que o app dele enfrentou (sem máquina, sem banco, sem log) | 24/09 (r29) | **relato de saúde**: o app conta sozinho (freio/credencial/falha/base vazia/fila presa) para a nuvem, e o **`/health` publica** (sem token, sem dado de negócio); o check-up mostra e leva no "Copiar resumo" | `cloudflare-worker/src/index.js`, `cloudflare_data_sync_patch.js`, `ajustes_v5227_*` | `test_worker_publico.js` (7 ✓) + `test_nuvem_explica.js` (3 ✓) + **aqui** |
| 27 | **"será que foi o gerar exe só nuvem?"** (verificado — **não é defeito**) | 24/09 (r29) | o `.exe` é um **lançador**: abre `https://teste-60f.pages.dev` no Edge/Chrome (`--app=`). Não tem sistema nem banco dentro e **não guarda dado**. O site publicado é o **mesmo build do repositório** (cabeçalho do bundle no ar conferido) | — | conferido em `nuvem/criar_exe_so_nuvem.ps1`, `nuvem/abrir_digicopy.vbs` + site publicado |
| 20 | **"bora focar somente na parte dos dados que não demonstram normalmente"** (o dono, 24/09, já com backup feito) — investigado: a tela pode ficar vazia em CINCO situações, e três delas eram **mudas** | 24/09 (r27) | **a faixa da nuvem**: avisa na tela, em português, sem conexão / pausada / limite do dia / erro grave / **nuvem com mais registros do que aqui** (com a conta: "clientes: 0 aqui × 3 na nuvem") e conserta em 1 clique (**Baixar tudo de novo**, relendo o diário desde o começo); o portão da conexão pode ser **reaberto** (o "jeito antigo" deixava a sessão vazia e sem aviso) | `ajustes_v7015_nuvem_explica_patch.js`, `ajustes_v5262_login_nuvem_primeiro_patch.js`, `cloudflare_data_sync_patch.js` | `test_nuvem_explica.js` (13 ✓) + **aqui** (11 travas) |
| 21 | (achado de desempenho da mesma rodada) o `info()` do motor percorria a base inteira e calculava o hash de cada registro **a cada chamada** — 223 ms numa base de 76 mil (e a faixa passou a chamar isso de 15 em 15 segundos) | 24/09 (r27) | a contagem (`pending`) virou **sob demanda** (getter): quem lê continua recebendo o número, quem não lê não paga. Medido: **223 ms → 0,05 ms** | `cloudflare_data_sync_patch.js` | `test_nuvem_explica.js` (2 verificações) + **aqui** |
| 19b | **melhoria tirada da 19:** base vazia **com a nuvem respondendo** avisava nada — a tela ficava muda e podia ser conexão apontando para outra loja (CNPJ errado) | 24/09 (r26) | aviso na tela, uma vez por abertura, **com o nome/CNPJ da empresa** da conexão; só dispara com a base inteira já trazida e a base vazia (sem alarme falso) | `cloudflare_data_sync_patch.js` | `test_nuvem_nao_perde.js` (2 verificações novas) + **aqui** |
| 28 | **"cadastra a impressora no contrato e ela some quando fecha e abre o programa"** — investigado: ele gravou e o programa fechou antes de subir (faltou luz, travou, sem internet, ou a fila não coube na despedida); ao reabrir, a fila pendente SUBIA e a nuvem confirmava, mas a confirmação só atualizava o livro-caixa e consumia a fila, SEM colocar o registro na base — o eco é pulado pelo guarda de versão ("já conheço esta versão") e o registro ficava na nuvem, mas INVISÍVEL no PC até a próxima reabertura. Segundo defeito da mesma família: a recusa da nuvem (`result.error`) era descartada SEM NENHUM AVISO | 25/09 (r30) | ao confirmar um upsert, se o registro NÃO está na base, ele entra com os dados que acabaram de subir (`materializarConfirmado` — nunca sobrescreve o que está na tela); recusa da nuvem agora avisa na hora (toast + sino + relato de saúde `recusado`) | `cloudflare_data_sync_patch.js` | `test_impressora_nao_some_reabrir.js` (7 ✓) |
| 29 | **"às vezes um dado criado não aparece em outro PC; demora sincronizar os dados para aparecer tudo de uma vez"** (a impressora do contrato era só um exemplo) — medido antes de mexer: com os dois PCs abertos o dado chega em ~1s (mecanismo são); o problema era (1) VENDAS e LEITURAS nunca se atualizavam sozinhas (fora das telas ao vivo — o dado chegava no banco, mas a lista na tela continuava velha), (2) a abertura relia o diário INTEIRO (60 mil mudanças = 69 idas e voltas) e a tela azul de carga segurava o programa até o fim | 25/09 (r31) | VENDAS e LEITURAS viraram telas ao vivo (CONFIG continua de fora de propósito: o render dela escreve nos campos e apagaria digitação não salva); a carga afina (faixinha, sem bloquear) assim que o estado atual chega; abertura nova lê a FOTO da nuvem (`GET /v1/snapshot`, motor 5.28.0: estado atual paginado pela chave + `snapshotSeq`, incremental dali em diante; motor antigo = o diário assume sozinho, sem dia de virada) | `cloudflare_data_sync_patch.js` `cloudflare-worker/src/index.js` | `test_dado_aparece_outro_pc.js` (27 ✓) `test_worker_publico.js` (+7 foto) |
| 30 | **"tem mais coisa de problema do que apenas isso, queria que você analisasse tudo, pois são vários problemas, eu não consigo identificar"** — varredura geral feita: botões, modais, impressão, nuvem, pausas, conflitos e ponte do `.exe` conferidos e sãos; achados: (1) app Android parado na v6.1.10 sem teste nem script (ele usa o PC primeiro — celular fica para depois, a pedido dele), (2) 650+ erros engolidos em silêncio (amostra: maioria guarda inofensiva), (3) 44 "esperas cegas" (defeito que só aparece na loja, no movimento) | 25/09 (r32) | ideia L: botão "Mandar o que quebrou" — 1 clique monta versão + tela + últimos 15 erros, SEM segredo (senha/token/Bearer viram ***), e abre o popup de copiar; mora no aviso de erro e no check-up da nuvem (NADA de botão no rodapé, por ordem dele) | `ajustes_v7020_mandar_erro_patch.js` `ajustes_v52239_avisos_erro_auditoria_patch.js` `ajustes_v5227_nuvem_acompanhamento_patch.js` | `test_mandar_erro.js` (21 ✓) |
| 19 | **"agora não está aparecendo nenhum dado, é normal?"** — investigado: não era defeito. Num **endereço novo** (navegador diferente, aba anônima, ou a cópia de teste da sessão) o sistema abre no **portão da nuvem** ("Este computador ainda não está conectado") e, pela regra 44 (nada salvo no PC), a base só vem da nuvem — então abre vazio **de propósito**; quem já conectou naquele navegador não vê o portão | 24/09 (r26) | nada a corrigir: é o comportamento certo (duas trancas: conexão da nuvem por endereço + login do usuário) | `ajustes_v5262_login_nuvem_primeiro_patch.js`, `cloudflare_data_sync_patch.js` | **aqui** (8 travas: portão cobre a tela, não reaparece para quem conectou, tela "Baixando os dados da nuvem…" com contagem e saída garantida) |

| 31 | **"dado que some/volta"** (dor nº 1 eleita por ele; r30–r31 já consertaram 3 causas provadas: confirmação que não entrava na base, recusa muda da nuvem, VENDAS/LEITURAS fora das telas ao vivo) — r33 prepara o terreno definitivo: (1) TRAVA contra redefinição silenciosa (o defeito mais caro: patch novo redefine função antiga e esquece o que ela fazia), (2) AUDITORIA arquivo por arquivo com prova de quem chama, (3) FAROL anti-estouro RESGATADO (estava vermelho: checava o número do plano grátis, obsoleto desde o plano pago da r28) | 25/09 (r33) | ideia D: foto das 635 redefinições velhas (`camadas_baseline.json`, perdoadas) + trava que só deixa redefinição NOVA passar com marcador de propósito ou encadeando a anterior; ideia B: `auditar_mortos.js` (492 .js na raiz, 227 no bundle, 20 órfãos dormentes MANTIDOS porque os alvos existem, 0 arquivos 100% mortos — nada apagado); farol atualizado para o freio do plano pago (1M/dia + 45M/mês) e registrado na suíte (roda sozinho em toda entrega) | `camadas_baseline.json` `auditar_mortos.js` `checar_cota_nuvem.js` | `test_sem_sobrescrita.js` (4 ✓) `checar_cota_nuvem.js` (farol verde, na suíte) |
| 32 | **auditoria externa que ele mandou executar** (prompt colado no chat): (1) 20 testes órfãos existiam mas ninguém rodava — a auditoria achou que era só o bloco de versão, mas 1 quebrava no harness (v5184 avaliava arquivo fóssil) e 5 tinham asserts de comportamento/cópia/tamanho podres junto; (2) `digicopy.branch` preso na sessão anterior (`arena/01a0cf4a-teste`, congelada na r29) — ZIP e links do ritual apontavam código velho de 5 rodadas | 25/09 (r34) | (1) 20 reparados: versão virou checagem viva (runtime) e carimbo virou consistência worker/motor — 0 órfãos, nada apagado; v5184 agora avalia o definidor vivo (v5189) e prende o layout lado a lado; (2) branch atualizada em 7 lugares (campo, trava, PASSO, BUILD_EXE, README do worker, 3 patches) + bundle regerado + app v7.0.21 | `package.json` `test_reclamacoes_do_dono.js` `BUILD_EXE.md` `PASSO_A_PASSO_NUVEM_E_SITE.html` | `test_ajustes_v5184.js` (9 ✓) `test_ajustes_v5245.js` `test_ajustes_v52419.js` |
## Linhas marcadas **aqui**

Estas reclamações **não tinham** teste próprio até a rodada 25 (a 15b foi um defeito **achado** por esta trava, não uma reclamação dele) — a trava delas mora dentro do
`test_reclamacoes_do_dono.js` (que também confere a tabela acima). São as que o dono mais repete quando
voltam: versão/branch errada, menu fiscal, modo escuro, a caixa retirada, o `erro.txt` do rodapé, o
SÓ NUVEM, o "dado que some" e o `prompt` que quebra no `.exe`.

## Reclamações que ainda NÃO têm conserto (ficam registradas, sem promessa)

Nenhuma nesta rodada. O que segue **em aberto** na auditoria (não são reclamações dele, são achados
nossos) está na `AUDITORIA_TECNICA.md` §35.7 e §38.4.
