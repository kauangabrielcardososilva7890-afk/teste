// ═══════════════════════════════════════════════════════════════════════════
// PATCH v4.9.47 — Leitura detalhada por departamento e lançamentos editáveis
// • Histórico de leituras só abre no duplo clique
// • Detalhe da leitura mostra impressoras lançadas, permite editar/remover
// • Novo lançamento mostra impressoras pendentes e remanejadas separadas
// • Impressão da leitura agrupa por departamento e soma subtotais/total geral
// ═══════════════════════════════════════════════════════════════════════════
(function(){
'use strict';

function txt(v){ return String(v ?? '').trim(); }
function low(v){ return txt(v).toLowerCase(); }
function n(v,fb=0){ const x=Number(String(v ?? '').replace(',','.')); return Number.isFinite(x)?x:fb; }
function cod(v){ const g=txt(v).match(/\d+/g); if(!g||!g.length) return ''; const c=g[g.length-1].replace(/^0+/,''); return c||'0'; }
function uidSafe(p){ return typeof uid==='function'?uid(p):`${p}_${Date.now()}_${Math.random().toString(36).slice(2,8)}`; }
function esc(v){ if(typeof escapeHtml==='function') return escapeHtml(v); return txt(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c])); }
function money(v){ return typeof fmtMoney==='function'?fmtMoney(n(v,0)):n(v,0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'}); }
function dataBR(v){ if(!txt(v)) return '—'; return typeof fmtDate==='function'?fmtDate(v):txt(v).slice(0,10); }
function sess(){ return typeof getSession==='function'?getSession():null; }
function salvar(){ if(typeof saveDB==='function') saveDB(); if(typeof renderDashboard==='function') setTimeout(()=>renderDashboard(),60); }
function cli(id){ return (db.clientes||[]).find(c=>c.id===id)||null; }
function eq(id){ return (db.equipamentos||[]).find(e=>e.id===id)||null; }
function ctr(id){ return (db.contratos||[]).find(c=>c.id===id)||null; }
function prq(id){ return (db.parque||[]).find(p=>p.id===id)||null; }
function toastMsg(m,t){ if(typeof toast==='function') toast(m,t||'info'); }
function seq(tipo, lista, empId){ return typeof proximoNumeroSimples==='function'?proximoNumeroSimples(tipo,lista||[],empId):String((lista||[]).filter(x=>x.empresaId===empId).length+1); }
function setModal(titulo, corpo, rodape, max='1080px'){
  const root=document.getElementById('modal-root'), box=document.getElementById('modal-box'); if(!root||!box) return;
  box.className=`w-full max-w-[${max}] rounded-[18px] bg-white shadow-2xl animate-slideIn overflow-hidden max-h-[94vh] flex flex-col`;
  document.getElementById('modal-title').innerText=titulo;
  document.getElementById('modal-body').innerHTML=corpo;
  document.getElementById('modal-footer').innerHTML=rodape||'';
  root.classList.remove('hidden');
}
const MEDS=[['pretoA4','Preto A4'],['pretoA3','Preto A3'],['colorA4','Color A4'],['colorA3','Color A3'],['scanner','Scanner']];
function normalizarModalidade(m){ const x=low(m); if(x==='global') return 'individual'; if(['individual','impressao','mes_fixo','inativo'].includes(x)) return x; if(x.includes('impress')) return 'impressao'; if(x.includes('fixo')||x.includes('mensal')) return 'mes_fixo'; return 'individual'; }
function medPadrao(key){ return {ativo:key==='pretoA4',ocultar:key!=='pretoA4',modalidade:key==='pretoA4'?'individual':'inativo',contadorInicial:0,acrescimo:0,franquia:0,valorFranquia:0,valorExcedente:0,valorPagina:0,valorLocacao:0}; }
function getMedidores(p){
  p.medidoresConfig=p.medidoresConfig||p.medidores||{};
  MEDS.forEach(([k])=>{ const old=p.medidoresConfig[k]||{}; p.medidoresConfig[k]=Object.assign(medPadrao(k),old,{modalidade:normalizarModalidade(old.modalidade||old.modo||medPadrao(k).modalidade)}); if(p.medidoresConfig[k].modalidade==='inativo') p.medidoresConfig[k].ativo=false; p.medidoresConfig[k].ocultar=!p.medidoresConfig[k].ativo; });
  p.medidores=p.medidoresConfig;
  return p.medidoresConfig;
}
function medAtivos(p){ return MEDS.map(([key,label])=>({key,label,...getMedidores(p)[key]})).filter(m=>m.ativo&&!m.ocultar&&m.modalidade!=='inativo'); }
function medPendentes(p,l){ return medAtivos(p).filter(m=>!(l.itens||[]).some(it=>it.parqueId===p.id && it.medidor===m.key)); }
function calc(m,ant,atu){ const usado=Math.max(0,n(atu)-n(ant)); if(m.modalidade==='impressao') return {usado,excedente:usado,valorTotal:usado*n(m.valorPagina)+n(m.acrescimo),valorUnitario:n(m.valorPagina)}; if(m.modalidade==='mes_fixo') return {usado,excedente:0,valorTotal:n(m.valorLocacao),valorUnitario:0}; const exc=Math.max(0,usado-n(m.franquia)); return {usado,excedente:exc,valorTotal:n(m.valorLocacao)+n(m.valorFranquia)+exc*n(m.valorExcedente)+n(m.acrescimo),valorUnitario:n(m.valorExcedente)}; }
function contratoLeitura(l){ return ctr(l.contratoId)||null; }
function maquinasContrato(c){ return (db.parque||[]).filter(p=>p.empresaId===c.empresaId&&p.contratoId===c.id); }
function maquinasAtivas(c){ return maquinasContrato(c).filter(p=>!['inativo','remanejada','remanejado','remanejado_outro'].includes(low(p.status||''))); }
function maquinasRemanejadas(c){ return maquinasContrato(c).filter(p=>['remanejada','remanejado','remanejado_outro'].includes(low(p.status||''))); }
function linhaLeitura(l){ const c=contratoLeitura(l); const cl=cli(l.clienteId||(c&&c.clienteId))||{}; const itens=l.itens||[]; const usado=itens.reduce((s,x)=>s+n(x.utilizado),0); const exc=itens.reduce((s,x)=>s+n(x.excedente),0); const total=itens.reduce((s,x)=>s+n(x.valorTotal),0)||n(l.valorTotal||l.valorExcedente); return {c,cl,itens,usado,exc,total}; }
function deptoItem(it){ return txt(it.departamento||it.setor||'Sem departamento')||'Sem departamento'; }
function agruparPorDepartamento(itens){
  const grupos={};
  (itens||[]).forEach(it=>{ const d=deptoItem(it); (grupos[d]=grupos[d]||{departamento:d,itens:[],utilizado:0,excedente:0,total:0}).itens.push(it); grupos[d].utilizado+=n(it.utilizado); grupos[d].excedente+=n(it.excedente); grupos[d].total+=n(it.valorTotal); });
  return Object.values(grupos).sort((a,b)=>a.departamento.localeCompare(b.departamento,'pt-BR',{sensitivity:'base'}));
}
function leituraBloqueada(l){ return ['faturado','finalizada','fechada'].includes(low(l&&l.status)); }

window.abrirLeiturasContrato=function(contratoId){
  const c=ctr(contratoId); if(!c) return; const cl=cli(c.clienteId)||{}; const list=(db.leituras||[]).filter(l=>l.contratoId===c.id).sort((a,b)=>new Date(b.dataLeitura||b.criadoEm||0)-new Date(a.dataLeitura||a.criadoEm||0));
  setModal(`Histórico de leituras — ${esc(cl.nome||'')}`,`<div class="space-y-4"><div class="rounded-xl bg-slate-50 border p-3"><b>Leituras realizadas</b><p class="text-[12px] text-slate-500">Dê duplo clique em uma leitura para abrir lançamentos, remover/editar e faturar.</p></div><div class="rounded-xl border overflow-hidden"><table class="w-full text-left text-[12.5px]"><thead class="bg-slate-50"><tr><th class="px-3 py-2">Código</th><th>Período</th><th>Lançada em</th><th>Utilizado</th><th>Exced.</th><th>Total</th><th>Status</th></tr></thead><tbody>${list.map(l=>{ const r=linhaLeitura(l); return `<tr ondblclick="abrirLeituraContratoDetalhe('${l.id}')" class="border-t hover:bg-slate-50 cursor-pointer"><td class="px-3 py-2"><b class="text-[#0a1e8a]">${esc(l.numero||cod(l.id))}</b><br><span class="text-[10px] text-slate-400">duplo clique</span></td><td>${dataBR(l.dataInicio)} a ${dataBR(l.dataFim)}</td><td>${dataBR(l.dataLeitura||l.criadoEm)}</td><td>${r.usado}</td><td>${r.exc}</td><td><b>${money(r.total)}</b></td><td><span class="neo-status ${l.status==='faturado'?'ok':'wait'}">${esc(l.status||'aberta')}</span></td></tr>`;}).join('')||'<tr><td colspan="7" class="text-center text-slate-400 py-8">Nenhuma leitura criada.</td></tr>'}</tbody></table></div></div>`,`<button onclick="openContratoCompleto('${c.id}')" class="neo-btn">Voltar ao contrato</button><button onclick="novaLeituraContrato('${c.id}')" class="neo-btn primary">Novo</button>`,'1040px');
};
window.novaLeituraContrato=function(contratoId){ const c=ctr(contratoId); if(!c) return; const s=sess(); const l={id:uidSafe('lei'),empresaId:c.empresaId,contratoId:c.id,clienteId:c.clienteId,numero:seq('leitura',db.leituras||[],c.empresaId),dataLeitura:new Date().toISOString(),dataInicio:new Date().toISOString().slice(0,10),dataFim:new Date().toISOString().slice(0,10),status:'aberta',itens:[],criadoPor:s&&s.usuarioId,criadoPorNome:s&&s.usuarioNome,criadoEm:new Date().toISOString()}; db.leituras=db.leituras||[]; db.leituras.push(l); salvar(); abrirLeituraContratoDetalhe(l.id); };
window.abrirLeituraContratoDetalhe=function(leituraId){
  const l=(db.leituras||[]).find(x=>x.id===leituraId); if(!l) return; const c=contratoLeitura(l); const r=linhaLeitura(l); const grupos=agruparPorDepartamento(l.itens||[]); const bloqueada=leituraBloqueada(l);
  setModal(`Leitura ${esc(l.numero||'')} — ${esc(r.cl.nome||'')}`,`<div class="space-y-4"><div class="grid grid-cols-1 md:grid-cols-4 gap-3"><div class="neo-card"><p class="neo-label">Total Utilizado</p><div class="neo-total">${r.usado}</div></div><div class="neo-card"><p class="neo-label">Total Excedentes</p><div class="neo-total">${r.exc}</div></div><div class="neo-card"><p class="neo-label">Soma Total</p><div class="neo-total">${money(r.total)}</div></div><div class="neo-card"><p class="neo-label">Status</p><div class="neo-total !text-[22px]">${esc(l.status||'aberta')}</div></div></div>${bloqueada?'<div class="rounded-xl bg-amber-50 border border-amber-200 p-3 text-[12px] text-amber-900"><b>Leitura faturada.</b> Para alterar lançamentos, estorne primeiro.</div>':''}<div class="rounded-xl border overflow-hidden"><div class="p-3 bg-slate-50 border-b flex justify-between"><b>Impressoras lançadas nesta leitura</b><button onclick="abrirLancamentoContador('${l.id}')" class="neo-btn primary !h-9 ${bloqueada?'opacity-50':''}" ${bloqueada?'disabled':''}><i class="ph ph-plus"></i>Novo lançamento</button></div>${grupos.map(g=>`<div class="border-b"><div class="bg-blue-50 px-3 py-2 font-bold text-[#0a1e8a]">Departamento: ${esc(g.departamento)} <span class="float-right text-slate-600">Usado ${g.utilizado} • Exced. ${g.excedente} • ${money(g.total)}</span></div><table class="w-full text-left text-[12px]"><thead class="bg-slate-50"><tr><th class="px-3 py-2">Impressora</th><th>Tipo</th><th>Serial</th><th>Anterior</th><th>Atual</th><th>Utilizado</th><th>Exced.</th><th>Total</th><th>Ações</th></tr></thead><tbody>${g.itens.map((it)=>{ const idx=(l.itens||[]).indexOf(it); return `<tr ondblclick="editarLancamentoLeitura('${l.id}',${idx})" class="border-t hover:bg-slate-50 cursor-pointer"><td class="px-3 py-2"><b>${esc(it.impressora)}</b><br><span class="text-[10px] text-slate-500">${esc(it.patrimonio||'')}</span></td><td>${esc(it.medidorLabel)}<br><span class="text-[10px] text-slate-500">${esc(it.modalidade)}</span></td><td>${esc(it.serial||'')}</td><td>${it.anterior}</td><td>${it.atual}</td><td>${it.utilizado}</td><td>${it.excedente}</td><td><b>${money(it.valorTotal)}</b></td><td><button onclick="event.stopPropagation(); editarLancamentoLeitura('${l.id}',${idx})" class="neo-btn !px-2" ${bloqueada?'disabled':''}><i class="ph ph-pencil"></i></button><button onclick="event.stopPropagation(); removerLancamentoLeitura('${l.id}',${idx})" class="neo-btn danger !px-2" ${bloqueada?'disabled':''}><i class="ph ph-trash"></i></button></td></tr>`; }).join('')}</tbody></table></div>`).join('')||'<div class="text-center text-slate-400 py-8">Nenhuma impressora lançada. Clique em Novo lançamento.</div>'}</div><div class="rounded-xl border bg-emerald-50 border-emerald-200 p-3"><b>Faturamento</b><p class="text-[12px] text-emerald-800">Valor para faturar: <b>${money(r.total)}</b>. Total utilizado: <b>${r.usado}</b>. Total excedente: <b>${r.exc}</b>.</p><button onclick="faturarLeituraContrato('${l.id}')" class="mt-2 neo-btn primary" ${bloqueada?'disabled':''}>Faturar esta leitura</button>${bloqueada?`<button onclick="estornarLeituraContrato('${l.id}')" class="mt-2 ml-2 neo-btn danger">Estornar leitura</button>`:''}</div></div>`,`<button onclick="abrirLeiturasContrato('${c.id}')" class="neo-btn">Voltar ao histórico</button><button onclick="imprimirLeituraContrato('${l.id}')" class="neo-btn primary">Imprimir notinha</button>`,'1180px');
};
function buscaHay(p){ const e=eq(p.equipamentoId)||{}; return {impressora:[e.modelo,e.descricao].join(' '),serial:e.serie,patrimonio:e.patrimonio||p.patrimonio,departamento:[p.setor,p.localInstalacao].join(' '),localizacao:[p.setor,p.localInstalacao].join(' ')}; }
function filtrarPendentes(leituraId){ const l=(db.leituras||[]).find(x=>x.id===leituraId); const c=contratoLeitura(l); const campo=document.getElementById('lan-filtro-campo')?.value||'impressora'; const q=low(document.getElementById('lan-filtro-texto')?.value||''); return maquinasAtivas(c).filter(p=>medPendentes(p,l).length>0).filter(p=>!q||low(buscaHay(p)[campo]).includes(q)); }
function optP(p){ const e=eq(p.equipamentoId)||{}; const ult=(db.leituras||[]).flatMap(l=>l.itens||[]).filter(it=>it.parqueId===p.id).slice(-1)[0]; const visita=(db.os||[]).filter(o=>o.parqueId===p.id||o.equipamentoId===p.equipamentoId).slice(-1)[0]; return `<option value="${p.id}">${esc(e.modelo||'Impressora')} | Serial ${esc(e.serie||'-')} | Patr ${esc(e.patrimonio||p.patrimonio||'-')} | ${esc(p.setor||'Geral')} / ${esc(p.localInstalacao||'-')} | Últ. leitura ${ult?ult.atual:'-'} | Últ. visita ${visita?dataBR(visita.dataAbertura||visita.criadoEm):'-'}</option>`; }
window.buscarImpressorasLancamento=function(leituraId){ const sel=document.getElementById('lan-prq'); if(!sel) return; const lista=filtrarPendentes(leituraId); sel.innerHTML='<option value="">Selecione</option>'+lista.map(optP).join(''); document.getElementById('lan-busca-info').innerText=`${lista.length} impressora(s) com medidor pendente`; if(lista.length===1){ sel.value=lista[0].id; atualizarTiposLancamento(); } };
window.abrirLancamentoContador=function(leituraId, idx=null){
  const l=(db.leituras||[]).find(x=>x.id===leituraId); if(!l) return; if(leituraBloqueada(l)) return toastMsg('Leitura faturada. Estorne para alterar.','error'); const edit=idx!=null && l.itens&&l.itens[idx]; const item=edit?l.itens[idx]:null; const c=contratoLeitura(l); const rem=maquinasRemanejadas(c);
  setModal(edit?'Editar lançamento':'Novo lançamento de contador',`<input type="hidden" id="lan-leitura-id" value="${l.id}"><input type="hidden" id="lan-edit-idx" value="${edit?idx:''}"><div class="space-y-4 text-[13px]"><div class="rounded-xl border bg-slate-50 p-3"><b>Buscar impressora</b><p class="text-[11px] text-slate-500">Busque por modelo, serial, patrimônio ou departamento/localização. Só aparecem medidores ainda pendentes.</p><div class="grid grid-cols-1 md:grid-cols-4 gap-2 mt-2"><select id="lan-filtro-campo" class="h-10 px-3 rounded-xl border"><option value="impressora">Impressora</option><option value="serial">Serial</option><option value="patrimonio">Patrimônio</option><option value="departamento">Departamento/Localização</option></select><input id="lan-filtro-texto" onkeydown="if(event.key==='Enter'){event.preventDefault(); buscarImpressorasLancamento('${l.id}') }" class="md:col-span-2 h-10 px-3 rounded-xl border" placeholder="Digite o termo da busca"><button onclick="buscarImpressorasLancamento('${l.id}')" class="h-10 px-4 rounded-xl bg-[#0a1e8a] text-white font-bold"><i class="ph ph-magnifying-glass"></i> Buscar</button></div><p id="lan-busca-info" class="text-[11px] text-slate-500 mt-2">${filtrarPendentes(leituraId).length} impressora(s) com medidor pendente</p></div><label class="font-bold text-slate-600">Impressora<select id="lan-prq" ${edit?'disabled':''} onchange="atualizarTiposLancamento()" class="mt-1 w-full h-10 px-3 rounded-xl border"><option value="">Selecione</option>${(edit?[prq(item.parqueId)]:filtrarPendentes(leituraId)).filter(Boolean).map(optP).join('')}</select></label><label class="font-bold text-slate-600">Tipo de impressão ativo<select id="lan-med" ${edit?'disabled':''} class="mt-1 w-full h-10 px-3 rounded-xl border"><option value="">Escolha a impressora</option></select></label><label class="font-bold text-slate-600">Contador atual<input id="lan-cont" type="number" value="${edit?item.atual:''}" autofocus class="mt-1 w-full h-12 px-3 rounded-xl border text-[18px] font-mono font-bold" placeholder="Digite somente o contador"></label>${rem.length?`<div class="rounded-xl border bg-amber-50 p-3"><b>Remanejadas neste contrato</b><p class="text-[11px] text-amber-800">Aparecem só para histórico e não podem receber leitura.</p>${rem.map(p=>{const e=eq(p.equipamentoId)||{}; return `<div class="mt-1 text-[12px]">${esc(e.patrimonio||p.patrimonio||'-')} — ${esc(e.modelo||'')}</div>`;}).join('')}</div>`:''}</div>`,`<button onclick="abrirLeituraContratoDetalhe('${l.id}')" class="neo-btn">Voltar</button><button onclick="salvarLancamentoContador('${l.id}')" class="neo-btn primary">Salvar lançamento</button>`,'820px');
  if(edit){ document.getElementById('lan-prq').value=item.parqueId; atualizarTiposLancamento(); document.getElementById('lan-med').value=item.medidor; }
};
window.atualizarTiposLancamento=function(){ const l=(db.leituras||[]).find(x=>x.id===document.getElementById('lan-leitura-id')?.value)||{itens:[]}; const idx=document.getElementById('lan-edit-idx')?.value; const p=prq(document.getElementById('lan-prq')?.value); const sel=document.getElementById('lan-med'); if(!sel) return; let meds=p?medPendentes(p,l):[]; if(idx!==''&&p){ const item=l.itens[Number(idx)]; const old=medAtivos(p).find(m=>m.key===item.medidor); if(old&&!meds.find(m=>m.key===old.key)) meds=[old,...meds]; } sel.innerHTML=p?meds.map(m=>`<option value="${m.key}">${esc(m.label)} — ${esc(m.modalidade)}</option>`).join('')||'<option value="">Todos os tipos ativos já lançados</option>':'<option value="">Escolha a impressora</option>'; };
window.editarLancamentoLeitura=function(leituraId,idx){ abrirLancamentoContador(leituraId,idx); };
window.removerLancamentoLeitura=function(leituraId,idx){ const l=(db.leituras||[]).find(x=>x.id===leituraId); if(!l||leituraBloqueada(l)) return toastMsg('Leitura faturada. Estorne para alterar.','error'); if(!confirm('Remover este lançamento? Ele voltará para pendente.')) return; l.itens.splice(idx,1); l.valorTotal=l.itens.reduce((s,x)=>s+n(x.valorTotal),0); l.valorExcedente=l.valorTotal; salvar(); abrirLeituraContratoDetalhe(leituraId); };
window.salvarLancamentoContador=function(leituraId){ const l=(db.leituras||[]).find(x=>x.id===leituraId); const p=prq(document.getElementById('lan-prq')?.value); if(!l||!p) return toastMsg('Selecione a impressora','error'); if(leituraBloqueada(l)) return toastMsg('Leitura faturada. Estorne para alterar.','error'); const key=document.getElementById('lan-med')?.value; const med=medAtivos(p).find(m=>m.key===key); if(!med) return toastMsg('Selecione um tipo ativo','error'); const atual=n(document.getElementById('lan-cont')?.value); const e=eq(p.equipamentoId)||{}; p.contadores=p.contadores||{}; const anterior=n(p.contadores[key] ?? med.contadorInicial); const r=calc(med,anterior,atual); p.contadores[key]=atual; const item={parqueId:p.id,equipamentoId:p.equipamentoId,impressora:`${e.patrimonio||''} ${e.modelo||''}`.trim(),modelo:e.modelo||'',patrimonio:e.patrimonio||p.patrimonio||'',serial:e.serie||'',departamento:p.setor||'Sem departamento',setor:p.setor||'Sem departamento',localizacao:p.localInstalacao||'',medidor:key,medidorLabel:med.label,modalidade:med.modalidade,franquia:n(med.franquia),valorUnitario:r.valorUnitario,anterior,atual,utilizado:r.usado,excedente:r.excedente,acrescimo:n(med.acrescimo),valorExcedente:n(med.valorExcedente),valorTotal:r.valorTotal}; const idx=document.getElementById('lan-edit-idx')?.value; l.itens=l.itens||[]; if(idx!=='') l.itens[Number(idx)]=item; else l.itens.push(item); l.valorTotal=l.itens.reduce((s,x)=>s+n(x.valorTotal),0); l.valorExcedente=l.valorTotal; salvar(); toastMsg('Lançamento salvo','success'); abrirLeituraContratoDetalhe(l.id); };
window.estornarLeituraContrato=function(leituraId){ const l=(db.leituras||[]).find(x=>x.id===leituraId); if(!l) return; if(!confirm('Estornar esta leitura para permitir edição?')) return; l.status='estornada'; (db.contasReceber||[]).forEach(c=>{ if(c.leituraId===l.id){ c.status='estornado'; c.pagamentoData=null; }}); salvar(); abrirLeituraContratoDetalhe(l.id); };
window.faturarLeituraContrato=function(leituraId){ const s=sess(); const l=(db.leituras||[]).find(x=>x.id===leituraId); if(!s||!l) return; const c=contratoLeitura(l); const r=linhaLeitura(l); let cr=(db.contasReceber||[]).find(x=>x.leituraId===l.id); const dados={empresaId:s.empresaId,origem:'leitura',clienteId:r.cl.id,descricao:`Leitura ${l.numero||''} — ${r.cl.nome||''}`,valor:r.total,vencimento:new Date().toISOString(),pagamentoData:null,status:'aberto',contratoId:c&&c.id,leituraId:l.id,vendaId:null,criadoPor:s.usuarioId,criadoPorNome:s.usuarioNome}; if(cr) Object.assign(cr,dados); else db.contasReceber.push({id:uidSafe('cr'),...dados}); l.status='faturado'; salvar(); toastMsg('Leitura faturada no financeiro','success'); abrirLeituraContratoDetalhe(l.id); };
window.imprimirLeituraContrato=function(leituraId){ const l=(db.leituras||[]).find(x=>x.id===leituraId); if(!l) return; const r=linhaLeitura(l); const c=r.c; const grupos=agruparPorDepartamento(l.itens||[]); const totalGeral={usado:r.usado,excedente:r.exc,total:r.total}; const html=`<!doctype html><html><head><meta charset="utf-8"><title>Leitura ${esc(l.numero)}</title><style>body{font-family:Arial;margin:24px;color:#111}h1{color:#0a1e8a;margin:0 0 6px}.top{border-bottom:3px solid #0a1e8a;padding-bottom:10px;margin-bottom:14px}.dept{margin-top:16px;border:1px solid #ccc}.dept h2{margin:0;background:#eaf0ff;color:#0a1e8a;font-size:15px;padding:8px}.item{border-top:1px solid #ddd;padding:8px 10px}.item-head{display:grid;grid-template-columns:90px 70px 1fr;background:#e5e5e5;border:1px solid #222;font-weight:bold;font-size:14px;padding:2px 8px}.grid{display:grid;grid-template-columns:1fr 1fr 1fr;gap:4px 18px;font-size:12px;margin-top:6px}.subtotal,.total{font-weight:bold;text-align:right;padding:8px}.total{font-size:20px;color:#0a1e8a}.no-print{margin-bottom:12px}@media print{.no-print{display:none}}</style></head><body><button class="no-print" onclick="window.print()">Imprimir / Salvar PDF</button><div class="top"><h1>DIGICOPY — Notinha de leitura</h1><div><b>Cliente:</b> ${esc(r.cl.nome||'')} • <b>Contrato:</b> ${esc(c&&c.numero||'')} • <b>Período:</b> ${esc(l.dataInicio||'')} a ${esc(l.dataFim||'')}</div></div>${grupos.map(g=>`<div class="dept"><h2>Departamento: ${esc(g.departamento)}</h2>${g.itens.map(it=>`<div class="item"><div class="item-head"><span>${esc((it.medidorLabel||'').replace(' A4','').replace(' A3','').toUpperCase())}</span><span>${esc(cod(it.patrimonio)||'')}</span><span>${esc(it.modelo||it.impressora||'')}</span></div><div class="grid"><div><b>Serial:</b> ${esc(it.serial||'')}</div><div><b>Vlr Qtde:</b> ${n(it.valorUnitario).toLocaleString('pt-BR',{minimumFractionDigits:2})}</div><div><b>Anterior:</b> ${it.anterior}</div><div><b>Depart:</b> ${esc(it.departamento||'')}</div><div><b>Excedeu:</b> ${it.excedente}</div><div><b>Qtde Atual:</b> ${it.atual}</div><div><b>Franquia:</b> ${it.franquia||0}</div><div><b>Vlr Exc:</b> ${n(it.valorExcedente).toLocaleString('pt-BR',{minimumFractionDigits:2})}</div><div><b>Utilizado:</b> ${it.utilizado}</div><div></div><div></div><div><b>Acrésc:</b> ${money(it.acrescimo||0)}<br><b>Vlr Exced:</b> ${money((it.excedente||0)*(it.valorExcedente||it.valorUnitario||0))}<br><b>Valor Total:</b> ${money(it.valorTotal)}</div></div></div>`).join('')}<div class="subtotal">Subtotal ${esc(g.departamento)} — Utilizado: ${g.utilizado} • Excedente: ${g.excedente} • ${money(g.total)}</div></div>`).join('')}<div class="total">TOTAL GERAL — Utilizado: ${totalGeral.usado} • Excedente: ${totalGeral.excedente} • ${money(totalGeral.total)}</div></body></html>`; const w=window.open('','_blank'); w.document.write(html); w.document.close(); };

window.LEITURA_DETALHADA_DEPARTAMENTOS_PURE={ agruparPorDepartamento, medPendentes, calc };
console.log('[DIGICOPY] leitura_detalhada_departamentos_patch.js v4.9.47 carregado');
})();
